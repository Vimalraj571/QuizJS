/* eslint-disable no-undef */
const { quiz } = require("../quiz");

describe("Simple Test", () => {
  test.todo("Need to wite test");
  test("Test for the Quiz app", () => {
    expect(quiz()).toBe(0);
  });
});
